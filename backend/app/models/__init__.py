"""SQLAlchemy ORM models.

Sprint 1: intentionally empty. Domain entities (devices, conversations, audit
events, provider configs) are added in later sprints via the `database` package
schema modules.
"""
